#!/bin/sh
#
# PROVIDE: portmap
# REQUIRE: LOGIN DAEMON NETWORKING
# KEYWORD: shutdown

$_rc_subr_loaded . /etc/rc.subr

name="portmap"
rcvar=$name
command="/sbin/$name"

load_rc_config $name
run_rc_command "$1"
