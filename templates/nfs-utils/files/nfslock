#!/bin/sh
#
# PROVIDE: nfslock
# REQUIRE: portmap
# KEYWORD: shutdown

$_rc_subr_loaded . /etc/rc.subr

name="nfslock"
rcvar=$name
command="/usr/sbin/rpc.statd"
pidfile="/var/run/rpc.statd.pid"

load_rc_config $name
run_rc_command "$1"
