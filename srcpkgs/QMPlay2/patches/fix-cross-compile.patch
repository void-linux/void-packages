--- a/lang/CMakeLists.txt
+++ b/lang/CMakeLists.txt
@@ -12,7 +12,7 @@
     endforeach()
 endif()
 
-find_package(${QT_PREFIX} REQUIRED COMPONENTS LinguistTools)
+find_package(${QT_PREFIX} REQUIRED COMPONENTS Linguist)
 if(BUILD_WITH_QT6)
     qt6_add_translation(QM_FILES ${QMPLAY2_TSS})
 else()
