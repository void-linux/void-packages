The cmake build system actually never checks for those headers,
we know they are available.
--- a/libde265/arm/arm.cc
+++ b/libde265/arm/arm.cc
@@ -51,7 +51,7 @@
 QPEL_FUNC(hevc_put_qpel_h3v3_neon_8);
 #undef QPEL_FUNC
 
-#if defined(HAVE_SIGNAL_H) && defined(HAVE_SETJMP_H)
+#if 1
 
 #include <signal.h>
 #include <setjmp.h>
