#!/bin/sh

[ -r ../conf ] && . ../conf

if [ "$LOGGING_ENABLED" -eq 1 ]; then
    if [ ! -d "$LOG_DIR" ]; then
        mkdir -p "$LOG_DIR"
    fi
    exec svlogd -tt "$LOG_DIR"
else
    sv down cloudflare-warp/log
fi
