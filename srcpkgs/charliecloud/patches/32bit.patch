--- a/bin/mem.c
+++ b/bin/mem.c
@@ -310,10 +310,10 @@
 #endif
    // time
    s = asprintf_ch("gc:  "
-         "%s: %ld collections (%+ld) in %zdms (%+zd)",
+         "%s: %ld collections (%+ld) in %jdms (%+jd)",
          when,
          ps.gc_no, ps.gc_no - gc_no_prev,
-         time_collecting, time_collecting - time_collecting_prev);
+         (intmax_t)time_collecting, (intmax_t)(time_collecting - time_collecting_prev));
    gc_no_prev = ps.gc_no;
    time_collecting_prev = time_collecting;
    DEBUG(s);
