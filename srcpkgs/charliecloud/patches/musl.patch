--- a/bin/fs.c
+++ b/bin/fs.c
@@ -13,6 +13,11 @@
 
 #include "all.h"
 
+/* FNM_EXTMATCH is a GNU extension to support extended globs in
+   fnmatch(3).  If not available, define as 0 to ignore this flag. */
+#ifndef HAVE_FNM_EXTMATCH
+#define FNM_EXTMATCH 0
+#endif
 
 /** Function prototypes (private) **/
 
