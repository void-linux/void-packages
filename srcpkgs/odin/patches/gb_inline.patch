diff --git a/src/gb/gb.h b/src/gb/gb.h
index 3c88ce6ed..648b745e5 100644
--- a/src/gb/gb.h
+++ b/src/gb/gb.h
@@ -465,18 +465,7 @@ typedef i32 b32; // NOTE(bill): Prefer this!!!
 	#endif
 #endif
 
-// TODO(bill): Should force inline be a separate keyword and gb_inline be inline?
-#if !defined(gb_inline)
-	#if defined(_MSC_VER)
-		#if _MSC_VER < 1300
 		#define gb_inline
-		#else
-		#define gb_inline __forceinline
-		#endif
-	#else
-		#define gb_inline inline __attribute__ ((__always_inline__))
-	#endif
-#endif
 
 #if !defined(gb_no_inline)
 	#if defined(_MSC_VER)
