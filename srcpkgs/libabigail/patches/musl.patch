--- a/tools/abisym.cc
+++ b/tools/abisym.cc
@@ -151,10 +151,10 @@
       cout << "could not find symbol '"
 	   << opts.symbol_name
 	   << "' in file '";
-      if (opts.absolute_path)
+      //if (opts.absolute_path)
 	cout << opts.elf_path << "'\n";
-      else
-	cout << basename(opts.elf_path);
+      //else
+	//cout << basename(opts.elf_path);
       return 0;
     }
 
