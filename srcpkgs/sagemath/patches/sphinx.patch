--- a/pyproject.toml
+++ b/pyproject.toml
@@ -68,7 +68,7 @@
   'ptyprocess > 0.5',
   'requests >=2.13.0',
   'scipy >=1.11',
-  'sphinx >=6.2, <9',
+  'sphinx >=6.2, <10',
   'sympy >=1.6, <2.0',
   # TODO: Remove this once the migration to meson is complete
   'pkgconfig',
