# This file provides the kernel cmdline options for the default uboot
# script. Change them as needed to boot your configuration.

# Add cmdline arguments when volume rocker buttons are pressed (comment out to disable)
#
# for example: cmdline will be `console=tty0 ... pp.volup pp.voldown` when both
# rocker buttons are pressed down
ROCKER_ARGS=yes

# Regular cmdline for configured kernel
CMDLINE="console=tty0 console=ttyS0,115200 rootwait loglevel=4"
