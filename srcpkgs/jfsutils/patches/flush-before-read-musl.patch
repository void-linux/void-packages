--- a/mkfs/mkfs.c
+++ b/mkfs/mkfs.c
@@ -924,6 +924,7 @@ int main(int argc, char *argv[])
 		}
 		do {
 			printf("\nContinue? (Y/N) ");
+                       fflush(stdout);
 			c = getchar();
 
 			if (c == 'n' || c == 'N') {
