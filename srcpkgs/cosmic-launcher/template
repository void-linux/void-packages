# Template file for 'cosmic-launcher'
pkgname=cosmic-launcher
version=1.5.0.20260729
revision=1
_commit="585a8c0c98d0385a91942c7f0e54d7ab209c1e79"
archs="x86_64* i686"
build_style="cargo"
configure_args="--all"
hostmakedepends="pkg-config just $(vopt_if sccache "rust-sccache")"
makedepends="libxkbcommon-devel which"
depends="libxkbcommon pop-launcher"
short_desc="COSMIC App Launcher"
maintainer="Bella109 <belladev109@proton.me>"
license="GPL-3.0-only"
homepage="https://github.com/pop-os/cosmic-launcher"
# Post epoch-1.5.0 snapshot: fixes the launcher flickering/closing when
# Ctrl is used as a modifier for window switching.
distfiles="https://github.com/pop-os/cosmic-launcher/archive/${_commit}.tar.gz"
checksum=3958346b87a738ef1a8075f0746aac62a70dcf4b89cef3704bd12678327ac560
build_options="sccache"

pre_build() {
	$(vopt_if sccache "export RUSTC_WRAPPER=/usr/bin/sccache")
}

do_install() {
	just rootdir="${DESTDIR}" cargo-target-dir="target/${RUST_TARGET}" install
}
