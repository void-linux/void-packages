# Template file for 'xbps-src-helper'
pkgname=xbps-src-helper
version=0.1
revision=1
bootstrap=yes
create_wrksrc=yes
short_desc="Helper programs for xbps-src"
maintainer="Érico Nogueira <ericonr@disroot.org>"
license="BSD-2-Clause"
homepage="https://voidlinux.org"

do_extract() {
	cp ${FILESDIR}/xbps-src-helper.c .
}

do_build() {
	make xbps-src-helper

	head -23 xbps-src-helper.c | tail -22 | sed 's@ \* *@@' > LICENSE
}

do_install() {
	vbin xbps-src-helper
	vlicense LICENSE
}
