#!/bin/sh

piddir="/var/run/nvidia-persistenced"
pidfile="${piddir}/nvidia-persistenced.pid"
if [ -r ${pidfile} ]; then
    pid=$(cat ${pidfile})
    kill ${pid}
    waitpid -t 1 -e ${pid}
    rm -r ${piddir} || true
fi
