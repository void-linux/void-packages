# Template file for 'libsmbios-utils'.
#
short_desc="${sourcepkg} utilities"
long_desc="${long_desc}

This package contains some sample binaries that use libsmbios."

Add_dependency run libsmbios

do_install()
{
	mkdir -p ${DESTDIR}/usr
	mv ${SRCPKGDESTDIR}/usr/sbin ${DESTDIR}/usr
}
