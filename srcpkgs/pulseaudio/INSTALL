case "${ACTION}" in
post)
	cat <<_EOF
=====================================================================

To make ALSA use PulseAudio globally in your system install the
`alsa-plugins` package.

=====================================================================a
_EOF
	;;
esac
