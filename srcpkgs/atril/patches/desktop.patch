--- a/backend/comics/meson.build
+++ b/backend/comics/meson.build
@@ -2,7 +2,7 @@ backend_desktop_conf = configuration_dat
 backend_desktop_conf.set('COMICS_MIME_TYPES', comic_mimetypes)
 
 backend_desktop = configure_file(
-    input: 'comicsdocument.atril-backend.in',
+    input: 'comicsdocument.atril-backend.desktop.in',
     output: 'comicsdocument.atril-backend',
     configuration: backend_desktop_conf,
     install: true,
@@ -31,19 +31,3 @@ shared_module(
     install: true,
     install_dir: backendsdir
 )
-
-
-
-test_name = 'test-ev-archive'
-
-test_sources = files(
-  'ev-archive.c',
-  'test-ev-archive.c',
-)
-
-executable(
-  test_name,
-  test_sources,
-  include_directories: include_dirs,
-  dependencies: comics_deps,
-)
--- a/backend/pixbuf/meson.build
+++ b/backend/pixbuf/meson.build
@@ -21,8 +21,8 @@ shared_module(
 )
 
 i18n.merge_file(
-  input: 'pixbuf.atril-backend.desktop.in',
-  output: 'pixbuf.atril-backend',
+  input: 'pixbufdocument.atril-backend.desktop.in',
+  output: 'pixbufdocument.atril-backend',
   type: 'desktop',
   po_dir: po_dir,
   install: true,
--- a/backend/dvi/mdvi-lib/meson.build
+++ b/backend/dvi/mdvi-lib/meson.build
@@ -45,6 +45,7 @@ mdvi_c_args = [
 ]
 
 mdvi_deps = [
+    glib,
     kpathsea,
 ]
 
