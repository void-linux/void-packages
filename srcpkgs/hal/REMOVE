#
# This script removes the HAL user/group.
#
case "${ACTION}" in
pre)
	if [ "$UPDATE" = "no" ]; then
		userdel hal &>/dev/null
		[ $? -eq 0 ] && echo "Removed hal system user/group."
	fi
	;;
post)
	;;
esac
