--- a/setup.py	2026-09-05 11:17:38.041432390 +0200
+++ b/setup.py	2026-09-05 11:25:07.836190224 +0200
@@ -17,7 +17,6 @@
 
 import os
 import sys
-import platform
 import re
 import tempfile
 import shutil
@@ -86,8 +85,8 @@
 class SABCToolsBuild(build_ext):
     def build_extension(self, ext: Extension):
         # Try to determine the architecture to build for
-        machine = platform.machine().lower()
-        IS_X86 = machine in ["i386", "i686", "x86", "i86pc", "x86_64", "x64", "amd64"]
+        machine = os.getenv("XBPS_TARGET_MACHINE")
+        IS_X86 = machine.startswith("i686") or machine.startswith("x86_64")
         IS_MACOS = sys.platform == "darwin"
         IS_ARM = machine.startswith("arm") or machine.startswith("aarch64")
         IS_AARCH64 = True
