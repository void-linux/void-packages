# Template file for 'mesa-libclc'
pkgname=mesa-libclc
version=22.1.8.3
revision=1
_llvmver="${version%%.*}"
build_style=cmake
configure_args="-DLLVM_DIR=/usr/lib/llvm/${_llvmver}/lib/cmake/llvm"
hostmakedepends="pkg-config clang${_llvmver} llvm${_llvmver} SPIRV-LLVM-Translator${_llvmver}-devel" # lld${_llvmver}
makedepends="llvm${_llvmver}-devel"
short_desc="Fork of LLVM's libclc applying some fixes of a known good state"
maintainer="Orphaned <orphan@voidlinux.org>"
license="Apache-2.0 AND NCSA"
homepage="https://gitlab.freedesktop.org/karolherbst/mesa-libclc"
distfiles="https://gitlab.freedesktop.org/karolherbst/mesa-libclc/-/archive/${version}/mesa-libclc-${version}.tar.gz"
checksum=d6fc17db2c4ae20ecfe04060af0572b2ab3701bb13822dab93efcaca0675205e

alternatives="
 libclc:/usr/share/clc:/usr/share/mesa-clc
 libclc:/usr/share/pkgconfig/libclc.pc:/usr/share/pkgconfig/mesa-libclc.pc"

if [ "$CROSS_BUILD" ]; then
	hostmakedepends+=" llvm${_llvmver}-cross-tools"
fi
