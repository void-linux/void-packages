#!/bin/sh
exec 2>&1
# Must run as root: owns com.system76.CosmicGreeter on the system bus
# and setuids into each user to load their configs/wallpapers.
sv check dbus >/dev/null || exit 1
install -d -m0755 -o cosmic-greeter -g cosmic-greeter /run/cosmic-greeter
exec cosmic-greeter-daemon
