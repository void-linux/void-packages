# don't use libc++
--- a/src/psd/CMakeLists.txt
+++ b/src/psd/CMakeLists.txt
@@ -10,10 +10,6 @@ set(CMAKE_CXX_STANDARD_REQUIRED ON)
 
 option(PSD_TOOLS "Compile psd tools" on)
 
-if(CMAKE_CXX_COMPILER_ID MATCHES "Clang")
-  set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -stdlib=libc++")
-endif()
-
 include_directories(.)
 
 add_library(psd
