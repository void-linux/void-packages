#!/bin/sh

fbuser=_firebird
fbgroup=_firebird
fbname=${PWD##*/}
fbglock=/var/run/${fbname}

install -d -m750 -o $fbuser -g $fbgroup "${fbglock}"

exec chpst -u $fbuser:$fbgroup /usr/lib/${fbname}/sbin/fbguard -forever
