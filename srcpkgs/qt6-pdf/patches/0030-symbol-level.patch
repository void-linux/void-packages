--- a/src/core/CMakeLists.txt
+++ b/src/core/CMakeLists.txt
@@ -615,6 +615,12 @@ foreach(arch ${archs})
             )
         endif()
 
+        if (DEFINED XBPS_SYMBOL_LEVEL)
+            list(APPEND gnArgArg
+                symbol_level=${XBPS_SYMBOL_LEVEL}
+            )
+        endif()
+
         add_gn_command(
             CMAKE_TARGET WebEngineCore
             NINJA_TARGETS QtWebEngineCore convert_dict
--- a/src/pdf/CMakeLists.txt
+++ b/src/pdf/CMakeLists.txt
@@ -244,6 +244,12 @@ foreach(arch ${archs})
             CONDITION QT_FEATURE_webengine_qt_freetype
         )
 
+        if (DEFINED XBPS_SYMBOL_LEVEL)
+            list(APPEND gnArgArg
+                symbol_level=${XBPS_SYMBOL_LEVEL}
+            )
+        endif()
+
         add_gn_command(
             CMAKE_TARGET Pdf
             NINJA_TARGETS QtPdf
