--- a/nss/coreconf/arch.mk
+++ b/nss/coreconf/arch.mk
@@ -55,6 +55,7 @@ ifeq (,$(filter-out Linux FreeBSD ,$(OS_
 endif
 
 ifeq ($(OS_ARCH),Linux)
+    OS_RELEASE := 6.1.0
     OS_RELEASE := $(subst ., ,$(OS_RELEASE))
     ifneq ($(words $(OS_RELEASE)),1)
 	OS_RELEASE := $(word 1,$(OS_RELEASE)).$(word 2,$(OS_RELEASE))
