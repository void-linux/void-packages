# Template file for 'ypbind'
pkgname=ypbind
version=2.7.2
revision=1
archs="~*-musl"
wrksrc="${pkgname}-mt-${version}"
build_style=gnu-configure
hostmakedepends="libnsl-devel libtirpc-devel pkg-config"
makedepends="libnsl-devel libtirpc-devel"
short_desc="Multithreaded daemon maintaining the NIS binding information"
maintainer="Anthony Iliopoulos <ailiop@altatus.com>"
license="GPL-2.0-only"
homepage="https://github.com/thkukuk/ypbind-mt"
distfiles="${homepage}/releases/download/v${version}/ypbind-mt-${version}.tar.xz"
checksum=064f2f185673c5493df83f6400b799f3a359de56118b6ba37c4327111f2fcd8b

post_install() {
	vsv ypbind
}
