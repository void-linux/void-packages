--- a/io.c
+++ b/io.c
@@ -1076,7 +1076,11 @@
 		if (DEBUG_GTE(IO, 1))
 			rprintf(FINFO, "[%s] send_msg_success(%d)\n", who_am_i(), num);
 
+#ifdef USE_STAT64_FUNCS
+		if (stat64(fname, &st) < 0)
+#else
 		if (stat(fname, &st) < 0)
+#endif
 			memset(&st, 0, sizeof (STRUCT_STAT));
 		SIVAL(num_dev_ino_buf, 0, num);
 		SIVAL64(num_dev_ino_buf, 4, st.st_dev);
--- a/syscall.c
+++ b/syscall.c
@@ -1172,7 +1172,11 @@
 	if (dfd < 0)
 		return -1;
 
+#ifdef USE_STAT64_FUNCS
+	ret = fstatat64(dfd, bname, st, at_flags);
+#else
 	ret = fstatat(dfd, bname, st, at_flags);
+#endif
 	e = errno;
 	close(dfd);
 	errno = e;
