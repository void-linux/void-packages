diff --git a/./a/UnixProcess.cpp b/./b/UnixProcess.cpp
index ba5e7f3..21e4019 100644
--- a/CodeLite/AsyncProcess/UnixProcess.cpp
+++ b/CodeLite/AsyncProcess/UnixProcess.cpp
@@ -37,7 +37,8 @@ UnixProcess::UnixProcess(wxEvtHandler* owner, const wxArrayString& args)
 
         // prevent descriptor leak into child process
 #ifdef SYS_close_range
-        close_range(3, ~0U, 0);
+//        close_range(3, ~0U, 0);
+        syscall(SYS_close_range, 3, ~0U, 0);
 #elif defined(SYS_closefrom)
         closefrom(3);
 #else
