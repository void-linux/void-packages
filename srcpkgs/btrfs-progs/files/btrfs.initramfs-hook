#!/bin/sh

PREREQ=""

prereqs()
{
	echo "$PREREQ"
}

case $1 in
# get pre-requisites
prereqs)
	prereqs
	exit 0
	;;
esac

. /usr/share/initramfs-tools/hook-functions

if [ -x /usr/bin/btrfs ]; then
	force_load btrfs
	copy_exec /usr/bin/btrfs sbin
fi

exit 0
