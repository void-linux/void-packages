# Template file for 'tiff'
pkgname=tiff
version=4.7.1
revision=2
build_style=gnu-configure
configure_args="--enable-cxx --without-x"
makedepends="jbigkit-devel libjpeg-turbo-devel
 liblzma-devel libzstd-devel zlib-devel libwebp-devel"
short_desc="Library and tools for reading and writing TIFF data files"
maintainer="skmpz <dem.procopiou@gmail.com>"
license="libtiff"
homepage="http://www.simplesystems.org/libtiff/"
distfiles="https://download.osgeo.org/libtiff/tiff-${version}.tar.gz"
checksum=f698d94f3103da8ca7438d84e0344e453fe0ba3b7486e04c5bf7a9a3fabe9b69

if [ "$XBPS_CHECK_PKGS" ]; then
	configure_args+=" --enable-tests"
else
	configure_args+=" --disable-tests"
fi

post_install() {
	for f in ${DESTDIR}/usr/share/man/man3/*.3tiff; do
		mv ${f} ${f%tiff}
	done
	vlicense LICENSE.md
}

tiff-devel_package() {
	depends="${makedepends} ${sourcepkg}>=${version}_${revision}"
	short_desc+=" - development files"
	pkg_install() {
		vmove usr/include
		vmove usr/lib/pkgconfig
		vmove "usr/lib/*.a"
		vmove "usr/lib/*.so"
		vmove usr/share/man/man3
		vmove usr/share/doc
	}
}
