#!/bin/sh
exec 2>&1
exec 1>&2

# In order to prevent unpredictable behaviour,
# we check whether 'power-profiles-daemon' is running
sv status power-profiles-daemon >/dev/null && exit 1

tlp init start || exit 1
exec chpst -b tlp pause
