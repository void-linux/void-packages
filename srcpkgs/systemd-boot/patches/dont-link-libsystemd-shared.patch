diff --git meson.build meson.build
index 56c98b9..db4e496 100644
--- meson.build
+++ meson.build
@@ -1889,9 +1889,8 @@ if conf.get('ENABLE_EFI') == 1 and conf.get('HAVE_BLKID') == 1
         exe = executable('bootctl',
                          'src/boot/bootctl.c',
                          include_directories : includes,
-                         link_with : [libshared],
+                         link_with : [libshared_static, libsystemd_static],
                          dependencies : [libblkid],
-                         install_rpath : rootlibexecdir,
                          install : true)
         public_programs += exe
 

