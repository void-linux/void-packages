_SET_PERM_FDB() {
local _n1=${VERSION%%.*}
local _securitydb="/var/lib/${PKGNAME}/system/security${_n1}.fdb"

chmod 660 -- "${_securitydb}"
chown _firebird:_firebird -- "${_securitydb}"
}

case "${ACTION}" in
post)
	if [ "$UPDATE" = "no" ]
	then
		_SET_PERM_FDB
	fi
	;;
esac
