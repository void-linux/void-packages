#!/bin/sh
exec 2>&1

# In order to prevent unpredictable behaviour,
# we check whether 'tlp' is running
sv status tlp >/dev/null && exit 1

sv check dbus >/dev/null || exit 1
exec /usr/libexec/power-profiles-daemon
