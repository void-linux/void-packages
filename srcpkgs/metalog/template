# Template file for 'metalog'
pkgname=metalog
version=2018510
revision=1
wrksrc=${pkgname}-${pkgname}-${version}
build_style=gnu-configure
configure_args="--sbindir=/usr/bin"
hostmakedepends="autoconf-archive automake libtool pkg-config"
makedepends="pcre-devel"
conf_files="/etc/metalog.conf"
short_desc="A modern replacement for syslogd and klogd"
maintainer="Juan RP <xtraeme@voidlinux.eu>"
license="GPL-2.0-only"
homepage="https://github.com/hvisage/metalog"
distfiles="https://github.com/hvisage/metalog/archive/${pkgname}-${version}.tar.gz"
checksum=a325df34c3ae77cb2096bce5d5c1130f00d0b9c4f4ddfa185f4e42cafaa7ca05

pre_configure() {
	autoreconf -i -f
}

post_install() {
	vsv metalog
}
