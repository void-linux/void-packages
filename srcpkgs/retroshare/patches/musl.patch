--- a/libretroshare/src/ft/ftfileprovider.cc
+++ b/libretroshare/src/ft/ftfileprovider.cc
@@ -196,7 +196,7 @@
 		/*
 		 * seek for base_loc 
 		 */
-        if(fseeko64(fd, base_loc, SEEK_SET) == -1)
+        if(fseeko(fd, base_loc, SEEK_SET) == -1)
         {
             #ifdef DEBUG_FT_FILE_PROVIDER
             std::cerr << "ftFileProvider::getFileData() Failed to seek. Data_size=" << data_size << ", base_loc=" << base_loc << " !" << std::endl;

--- a/libretroshare/src/pgp/pgphandler.cc
+++ b/libretroshare/src/pgp/pgphandler.cc
@@ -1918,13 +1918,13 @@
 
 bool PGPHandler::locked_syncPublicKeyring()
 {
-	struct stat64 buf ;
+	struct stat buf ;
 #ifdef WINDOWS_SYS
 	std::wstring wfullname;
 	librs::util::ConvertUtf8ToUtf16(_pubring_path, wfullname);
 	if(-1 == _wstati64(wfullname.c_str(), &buf))
 #else
-	if(-1 == stat64(_pubring_path.c_str(), &buf))
+	if(-1 == stat(_pubring_path.c_str(), &buf))
 #endif
 		std::cerr << "PGPHandler::syncDatabase(): can't stat file " << _pubring_path << ". Can't sync public keyring." << std::endl;
 
@@ -1962,13 +1962,13 @@
 
 bool PGPHandler::locked_syncTrustDatabase()
 {
-	struct stat64 buf ;
+	struct stat buf ;
 #ifdef WINDOWS_SYS
 	std::wstring wfullname;
 	librs::util::ConvertUtf8ToUtf16(_trustdb_path, wfullname);
 	if(-1 == _wstati64(wfullname.c_str(), &buf))
 #else
-		if(-1 == stat64(_trustdb_path.c_str(), &buf))
+		if(-1 == stat(_trustdb_path.c_str(), &buf))
 #endif
 		{
 			std::cerr << "PGPHandler::syncDatabase(): can't stat file " << _trustdb_path << ". Will force write it." << std::endl;

--- a/libretroshare/src/ft/ftfilecreator.cc
@@ -66,7 +66,7 @@
 	rstime_t now = time(NULL) ;
 	_creation_time = now ;

-	struct stat64 buf;
+	struct stat buf;

 	// Initialise last recv time stamp to last modification time for the partial file.
 	//
@@ -75,7 +75,7 @@
 	librs::util::ConvertUtf8ToUtf16(file_name, wfullname);
 	if ( 0 == _wstati64(wfullname.c_str(), &buf))
 #else
-	if ( 0 == stat64(file_name.c_str(), &buf))
+	if ( 0 == stat(file_name.c_str(), &buf))
 #endif
 		_last_recv_time_t = buf.st_mtime ;
 	else
@@ -212,7 +212,7 @@
 		/*
 		 * go to the offset of the file
 		 */
-		if (0 != fseeko64(this->fd, offset, SEEK_SET))
+		if (0 != fseeko(this->fd, offset, SEEK_SET))
 		{
 			std::cerr << "ftFileCreator::addFileData() Bad fseek at offset " << offset << ", fd=" << (void*)(this->fd) << ", size=" << mSize << ", errno=" << errno << std::endl;
 			return 0;
@@ -733,7 +733,7 @@
 	unsigned char *buff = new unsigned char[chunk_size] ;
 	uint32_t len ;

-	if(fseeko64(fd,(uint64_t)chunk_number * (uint64_t)chunk_size,SEEK_SET)==0 && (len = fread(buff,1,chunk_size,fd)) > 0)
+	if(fseeko(fd,(uint64_t)chunk_number * (uint64_t)chunk_size,SEEK_SET)==0 && (len = fread(buff,1,chunk_size,fd)) > 0)
 	{
 		Sha1CheckSum comp = RsDirUtil::sha1sum(buff,len) ;

--- a/libretroshare/src/ft/ftdatamultiplex.cc
+++ b/libretroshare/src/ft/ftdatamultiplex.cc
@@ -710,7 +710,7 @@
 		return false ;
 	}
 	uint32_t len ;
-	if(fseeko64(fd,(uint64_t)chunk_number * (uint64_t)ChunkMap::CHUNKMAP_FIXED_CHUNK_SIZE,SEEK_SET)!=0 || 0==(len = fread(buf,1,ChunkMap::CHUNKMAP_FIXED_CHUNK_SIZE,fd))) 
+	if(fseeko(fd,(uint64_t)chunk_number * (uint64_t)ChunkMap::CHUNKMAP_FIXED_CHUNK_SIZE,SEEK_SET)!=0 || 0==(len = fread(buf,1,ChunkMap::CHUNKMAP_FIXED_CHUNK_SIZE,fd)))
 	{
 		std::cerr << "Cannot fseek/read from file " << filename << " at position " << (uint64_t)chunk_number * (uint64_t)ChunkMap::CHUNKMAP_FIXED_CHUNK_SIZE << std::endl;
 		fclose(fd) ;

@@ -346,14 +346,14 @@
         return (CopyFileW(sourceW.c_str(), destW.c_str(), FALSE) != 0);

 #else
-	FILE *in = fopen64(source.c_str(),"rb") ;
+	FILE *in = fopen(source.c_str(),"rb") ;

 	if(in == NULL)
 	{
 		return false ;
 	}

-	FILE *out = fopen64(dest.c_str(),"wb") ;
+	FILE *out = fopen(dest.c_str(),"wb") ;

 	if(out == NULL)
 	{
@@ -409,8 +409,8 @@
 	val = _wstat(wfilename.c_str(), &buf);
 	st_mode = buf.st_mode;
 #else
-	struct stat64 buf;
-	val = stat64(filename.c_str(), &buf);
+	struct stat buf;
+	val = stat(filename.c_str(), &buf);
 	st_mode = buf.st_mode;
 #endif
 	if (val == -1)
@@ -605,9 +605,9 @@
 	//unsigned char gblBuf[512];

 	/* determine size */
- 	fseeko64(fd, 0, SEEK_END);
-	size = ftello64(fd);
-	fseeko64(fd, 0, SEEK_SET);
+ 	fseeko(fd, 0, SEEK_END);
+	size = ftello(fd);
+	fseeko(fd, 0, SEEK_SET);

 	/* check if thread is running */
 	bool isRunning = thread ? thread->isRunning() : true;
@@ -826,7 +826,7 @@

 	return _wfopen(wfilename.c_str(), wmode.c_str());
 #else
-	return fopen64(filename, mode);
+	return fopen(filename, mode);
 #endif
 }

@@ -1292,15 +1292,15 @@
 	/***** XXX TO MAKE WIDE SYSTEM CALL ******************************************************/
 	std::string fp(filepath.begin(), filepath.end());

-	if (NULL == (fd = fopen64(fp.c_str(), "rb")))
+	if (NULL == (fd = fopen(fp.c_str(), "rb")))
 		return false;

 	/***** XXX TO MAKE WIDE SYSTEM CALL ******************************************************/

 	/* determine size */
- 	fseeko64(fd, 0, SEEK_END);
-	size = ftello64(fd);
-	fseeko64(fd, 0, SEEK_SET);
+ 	fseeko(fd, 0, SEEK_END);
+	size = ftello(fd);
+	fseeko(fd, 0, SEEK_SET);

 	SHA1_Init(sha_ctx);
 	while((len = fread(gblBuf,1, 512, fd)) > 0)

--- a/libretroshare/src/rsserver/rsaccounts.cc
+++ b/libretroshare/src/rsserver/rsaccounts.cc
@@ -534,7 +534,7 @@
 		return false ;
 	}

-	struct stat64 buf;
+	struct stat buf;

     for(;dirIt.isValid();dirIt.next())
 	{
@@ -550,7 +550,7 @@
 		librs::util::ConvertUtf8ToUtf16(fullname, wfullname);
 		if (-1 != _wstati64(wfullname.c_str(), &buf))
 #else
-		if (-1 != stat64(fullname.c_str(), &buf))
+		if (-1 != stat(fullname.c_str(), &buf))
 #endif

 		{

--- a/libretroshare/src/util/folderiterator.cc
+++ b/libretroshare/src/util/folderiterator.cc
@@ -50,14 +50,14 @@

     // Grab the last modification time for the directory

-    struct stat64 buf ;
+    struct stat buf ;

 #ifdef WINDOWS_SYS
     std::wstring wfullname;
     librs::util::ConvertUtf8ToUtf16(folderName, wfullname);
     if ( 0 == _wstati64(wfullname.c_str(), &buf))
 #else
-    if ( 0 == stat64(folderName.c_str(), &buf))
+    if ( 0 == stat(folderName.c_str(), &buf))
 #endif
     {
         mFolderModTime = buf.st_mtime ;
@@ -153,7 +153,7 @@
    }
 #endif

-   struct stat64 buf ;
+   struct stat buf ;

 #ifdef DEBUG_FOLDER_ITERATOR
    std::cerr << "FolderIterator: next. Looking into file " << mFileName ;
@@ -164,7 +164,7 @@
    librs::util::ConvertUtf8ToUtf16(mFullPath, wfullname);
    if ( 0 == _wstati64(wfullname.c_str(), &buf))
 #else
-   if ( 0 == stat64(mFullPath.c_str(), &buf))
+   if ( 0 == stat(mFullPath.c_str(), &buf))
 #endif
    {
 	  mFileModTime = buf.st_mtime ;

--- a/libretroshare/src/util/rsdiscspace.cc
+++ b/libretroshare/src/util/rsdiscspace.cc
@@ -106,9 +106,9 @@
 	free_blocks = buf.f_bavail;
 	block_size = buf.f_frsize ;
 #else
-	struct statvfs64 buf;
+	struct statvfs buf;
 	
-	if (0 != statvfs64 (file, &buf))
+	if (0 != statvfs (file, &buf))
 	{
 		std::cerr << "Size estimate failed for file " << file << std::endl ;
 		return false;

--- a/libretroshare/src/util/rsdir.cc
+++ b/libretroshare/src/util/rsdir.cc
@@ -345,14 +345,14 @@
         return (CopyFileW(sourceW.c_str(), destW.c_str(), FALSE) != 0);
 
 #else
-	FILE *in = fopen64(source.c_str(),"rb") ;
+	FILE *in = fopen(source.c_str(),"rb") ;
 
 	if(in == NULL)
 	{
 		return false ;
 	}
 
-	FILE *out = fopen64(dest.c_str(),"wb") ;
+	FILE *out = fopen(dest.c_str(),"wb") ;
 
 	if(out == NULL)
 	{
@@ -408,8 +408,8 @@
 	val = _wstat(wfilename.c_str(), &buf);
 	st_mode = buf.st_mode;
 #else
-	struct stat64 buf;
-	val = stat64(filename.c_str(), &buf);
+	struct stat buf;
+	val = stat(filename.c_str(), &buf);
 	st_mode = buf.st_mode;
 #endif
 	if (val == -1)
@@ -604,9 +604,9 @@
 	//unsigned char gblBuf[512];
 
 	/* determine size */
- 	fseeko64(fd, 0, SEEK_END);
-	size = ftello64(fd);
-	fseeko64(fd, 0, SEEK_SET);
+ 	fseeko(fd, 0, SEEK_END);
+	size = ftello(fd);
+	fseeko(fd, 0, SEEK_SET);
 
 	/* check if thread is running */
 	bool isRunning = thread ? thread->isRunning() : true;
@@ -825,7 +825,7 @@
 
 	return _wfopen(wfilename.c_str(), wmode.c_str());
 #else
-	return fopen64(filename, mode);
+	return fopen(filename, mode);
 #endif
 }
 
@@ -1291,15 +1291,15 @@
 	/***** XXX TO MAKE WIDE SYSTEM CALL ******************************************************/
 	std::string fp(filepath.begin(), filepath.end());
 
-	if (NULL == (fd = fopen64(fp.c_str(), "rb")))
+	if (NULL == (fd = fopen(fp.c_str(), "rb")))
 		return false;
 
 	/***** XXX TO MAKE WIDE SYSTEM CALL ******************************************************/
 
 	/* determine size */
- 	fseeko64(fd, 0, SEEK_END);
-	size = ftello64(fd);
-	fseeko64(fd, 0, SEEK_SET);
+ 	fseeko(fd, 0, SEEK_END);
+	size = ftello(fd);
+	fseeko(fd, 0, SEEK_SET);
 
 	SHA1_Init(sha_ctx);
 	while((len = fread(gblBuf,1, 512, fd)) > 0)
