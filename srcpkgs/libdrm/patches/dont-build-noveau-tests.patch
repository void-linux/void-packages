--- a/tests/meson.build
+++ b/tests/meson.build
@@ -44,9 +44,9 @@ endif
 if with_etnaviv
   subdir('etnaviv')
 endif
-if with_nouveau
-  subdir('nouveau')
-endif
+#if with_nouveau
+#  subdir('nouveau')
+#endif
 
 drmsl = executable(
   'drmsl',
