# Template file for 'gimp'
pkgname=gimp
version=2.10.9999
revision=1
build_style=meta
depends="gimp${version%%.*}"
short_desc="GNU image manipulation program (metapackage)"
maintainer="classabbyamp <void@placeviolette.net>"
license="GPL-3.0-only"
homepage="https://gimp.org"

libgimp_package() {
	build_style=meta
	short_desc+=" - runtime libraries"
	depends="libgimp${version%%.*}"
}

gimp-devel_package() {
	build_style=meta
	short_desc+=" - development files"
	depends="gimp${version%%.*}-devel"
}
