service exec
{
        flags                   = REUSE
        socket_type             = stream
        wait                    = no
        user                    = root
        server                  = /usr/bin/rexecd
        log_on_failure          += USERID
        disable                 = yes
}
