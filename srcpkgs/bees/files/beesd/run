#!/bin/sh
exec 2>&1

[ -r /etc/bees.conf ] && . /etc/bees.conf

if [ "$(uuidparse -n -o VARIANT "$UUID")" = "invalid" ]; then
	echo "Set UUID in /etc/bees.conf before starting beesd"
	sv down .
	exit 1
fi

exec /usr/bin/beesd --no-timestamps "$UUID"
