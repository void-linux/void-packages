Fails to handle our kernel versions that include the revision:
fatal error: failed to parse kernel version from uname
--- a/go/src/runtime/os_linux.go
+++ b/go/src/runtime/os_linux.go
@@ -965,7 +965,7 @@
 	println(rel)
 	// Strip anything after a dash or plus.
 	for i := 0; i < len(rel); i++ {
-		if rel[i] == '-' || rel[i] == '+' {
+		if rel[i] == '-' || rel[i] == '+' || rel[i] == '_' {
 			rel = rel[:i]
 			break
 		}
