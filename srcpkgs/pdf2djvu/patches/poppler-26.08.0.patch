--- a/pdf-backend.cc
+++ b/pdf-backend.cc
@@ -235,15 +235,19 @@ void pdf::Document::get_page_size(int n,
 
 const std::string pdf::Document::get_xmp()
 {
+#if POPPLER_VERSION_INT >= 260800
+  std::optional<std::string> mstring;
+#else
   std::unique_ptr<const pdf::String> mstring;
+#endif
 #if POPPLER_VERSION_INT >= 211000
   mstring = this->readMetadata();
 #else
   mstring.reset(this->readMetadata());
 #endif
-  if (mstring.get() == nullptr)
+  if (!mstring)
     return "";
-  const char *cstring = pdf::get_c_string(mstring.get());
+  const char *cstring = pdf::get_c_string(mstring);
   if (strncmp(cstring, "<?xpacket begin=", 16) != 0)
     return "";
   cstring += 16;
@@ -640,17 +644,23 @@ namespace pdf
   }
 }
 
-#if POPPLER_VERSION_INT >= 7200
-const char * pdf::get_c_string(const pdf::String *str)
+const char * pdf::get_c_string(std::optional<std::string> &str)
 {
-  return str->c_str();
+  return str->data();
 }
-#else
+
 const char * pdf::get_c_string(const pdf::String *str)
 {
+#if POPPLER_VERSION_INT >= 7200
+  return str->c_str();
+#else
   return str->getCString();
-}
 #endif
+}
+const char * pdf::get_c_string(std::unique_ptr<const pdf::String>& str)
+{
+  return pdf::get_c_string(str.get());
+}
 
 int pdf::find_page(pdf::Catalog *catalog, pdf::Ref pgref)
 {
--- a/pdf-backend.hh
+++ b/pdf-backend.hh
@@ -428,6 +428,8 @@ namespace pdf
 
   double get_path_area(pdf::splash::Path &path);
 
+  const char * get_c_string(std::optional<std::string> &str);
+  const char * get_c_string(std::unique_ptr<const pdf::String>& str);
   const char * get_c_string(const pdf::String *str);
 
   int find_page(pdf::Catalog *catalog, pdf::Ref pgref);
