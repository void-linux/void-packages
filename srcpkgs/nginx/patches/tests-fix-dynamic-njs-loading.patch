--- a/tests/lib/Test/Nginx.pm
+++ b/tests/lib/Test/Nginx.pm
@@ -740,6 +740,10 @@
 	$s .= "load_module $modules/ngx_stream_geoip_module.so;\n"
 		if $self->has_module('stream_geoip\S+=dynamic');
 
+	$s .= "load_module $modules/ngx_http_js_module.so;\n";
+
+	$s .= "load_module $modules/ngx_stream_js_module.so;\n";
+
 	return $s;
 }
 
