basename() is declared in <libgen.h>. glibc also exposes a GNU variant
through <cstring>, but musl does not, so the tests fail to build there.

--- a/src/tests/imageTests.cpp
+++ b/src/tests/imageTests.cpp
@@ -12,6 +12,9 @@
 #include <fstream>
 #include <map>
 #include <sys/stat.h>
+#if !defined(_MSC_VER)
+#include <libgen.h> // basename()
+#endif
 
 #include <png.h>
 
