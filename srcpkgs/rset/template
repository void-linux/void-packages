# Template file for 'rset'
pkgname=rset
version=2.1
revision=1
build_style=configure
make_install_args="PREFIX=/usr"
make_check_target=test
hostmakedepends="gcc"
checkdepends="ruby>=2.4 wget"
short_desc="Configure systems using using any scripting language"
maintainer="Benjamin Lee <benjamin@typ3.tech>"
license="ISC"
homepage="http://scriptedconfiguration.org/"
distfiles="https://github.com/eradman/rset/archive/refs/tags/${version}.tar.gz"
checksum=0bb66618d684e0f1b525696971991f498a27d28398c80b85651351a74c722624
# Currently, one of the rset tests is broken when running as root. When this
# isue is fixed on a later release, make_check can be set back to "yes".
# See: https://github.com/eradman/rset/issues/14
make_check=no
