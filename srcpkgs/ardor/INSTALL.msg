In order to run `ardor-tor`, TOR needs to be running and you have to copy the file `run-tor.sh` from `/usr/lib/nxt/` (after installing `nxt`) to `/usr/lib/ardor/`.
In order to install TOR, run `xbps-install -S tor` and then preferably start the tor service.
