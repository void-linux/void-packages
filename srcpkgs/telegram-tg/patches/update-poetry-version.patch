Fix build for new poetry version

--- a/pyproject.toml
+++ b/pyproject.toml
@@ -28,7 +28,7 @@
 tg = "tg.__main__:main"
 
 [build-system]
-requires = ["poetry-core>=1.0.0,<2.0.0"]
+requires = ["poetry-core>=1.0.0,<2.2.0"]
 build-backend = "poetry.core.masonry.api"
 
 [tool.mypy]
