Don't call g_thread_unref after g_thread_join, g_thread_join already calls g_thread_unref.
https://docs.gtk.org/glib/struct.Thread.html

diff -rup gupnp-1.6.7.orig/tests/test-service-proxy.c gupnp-1.6.7/tests/test-service-proxy.c
--- gupnp-1.6.7.orig/tests/test-service-proxy.c	2024-09-23 09:30:10.000000000 -0700
+++ gupnp-1.6.7/tests/test-service-proxy.c	2025-03-29 01:28:30.630530094 -0700
@@ -517,8 +517,6 @@ test_sync_call (ProxyTestFixture *tf, gc
         // Spin the loop for a bit...
         g_timeout_add (500, (GSourceFunc) delayed_loop_quitter, tf->loop);
         g_main_loop_run (tf->loop);
-
-        g_thread_unref (t);
 }
 
 gboolean
@@ -562,7 +560,6 @@ test_cancel_sync_call (ProxyTestFixture
         // Spin the loop for a bit...
         g_timeout_add (500, (GSourceFunc) delayed_loop_quitter, tf->loop);
         g_main_loop_run (tf->loop);
-        g_thread_unref (t);
         g_object_unref (d.cancellable);
 }
 
@@ -642,7 +639,6 @@ test_finish_soap_error_sync (ProxyTestFi
         // Spin the loop for a bit...
         g_timeout_add (500, (GSourceFunc) delayed_loop_quitter, tf->loop);
         g_main_loop_run (tf->loop);
-        g_thread_unref (t);
 }
 
 void
