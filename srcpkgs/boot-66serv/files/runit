[main]
@type = longrun
@version = @VERSION@
@description = "runit"
@user = (root)
@options = ( log )

[start]
@execute = ( /etc/runit/2 )

[stop]
@execute = ( exec-cmdline -s { sv force-stop /var/service/*; sv exit /var/service/* } )

[logger]
@backup = 3 
@maxsize = 1000000
