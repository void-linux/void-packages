# Template file for 'hyprwire'
pkgname=hyprwire
version=0.3.0
revision=3
build_style=cmake
hostmakedepends="
	hyprwayland-scanner
	pkgconf
	wayland-devel
	wayland-protocols
"
makedepends="
	hyprutils
	libffi-devel
	pugixml-devel
"
short_desc="Fast and consistent wire protocol for IPC"
maintainer="notnull <notnull@anarchyplanet.org>"
license="BSD-3-Clause"
homepage="https://github.com/hyprwm/hyprwire"
changelog="https://github.com/hyprwm/${pkgname}/releases"
distfiles="https://github.com/hyprwm/${pkgname}/archive/refs/tags/v${version}.tar.gz"
checksum=ba3ff4b82620209d5680ce0e771d0f2c41f78cdb96d5d84fa66e4f0709ca8de6

subpackages="${pkgname}-devel"

configure_args+=" --no-warn-unused-cli"
configure_args+=" -DCMAKE_BUILD_TYPE:STRING=Release"
configure_args+=" -DNO_SYSTEMD:BOOL=true"

post_install() {
	vlicense LICENSE
}

hyprwire-devel_package() {
	depends="${sourcepkg}>=${version}_${revision}"
	short_desc+=" - development files"
	pkg_install() {
		vmove usr/include
		vmove usr/lib/cmake/hyprwire-scanner
		vmove usr/lib/libhyprwire.so
		vmove usr/lib/pkgconfig/hyprwire-scanner.pc
		vmove usr/lib/pkgconfig/hyprwire.pc
	}
}
