# Template file for 'gnome-shell-extensions-dock'.
#
short_desc="Dock extension for GNOME Shell"

Add_dependency run gnome-shell-extensions

noarch=yes

do_install()
{
	cd ${wrksrc}/extensions/dock
	make DESTDIR=${DESTDIR} install
}
