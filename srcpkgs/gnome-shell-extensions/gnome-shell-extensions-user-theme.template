# Template file for 'gnome-shell-extensions-user-theme'.
#
short_desc="User theme extension for GNOME Shell"

Add_dependency run gnome-shell-extensions

triggers="gsettings-schemas"
noarch=yes

do_install()
{
	cd ${wrksrc}/extensions/user-theme
	make DESTDIR=${DESTDIR} install
}
