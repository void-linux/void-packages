# Template file for 'gnome-shell-extensions-automove-windows'.
#
short_desc="Automove windows extension for GNOME Shell"

Add_dependency run gnome-shell-extensions

triggers="gsettings-schemas"

noarch=yes

do_install()
{
	cd ${wrksrc}/extensions/auto-move-windows
	make DESTDIR=${DESTDIR} install
}
