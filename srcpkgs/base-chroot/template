# Template file for 'base-chroot'
pkgname=base-chroot
version=0.66
revision=1
bootstrap=yes
build_style=meta
short_desc="Bootstrap packages for xbps-src"
maintainer="Juan RP <xtraeme@voidlinux.org>"
license="Public domain"
homepage="http://www.voidlinux.org"
replaces="base-chroot-musl>=0"

depends="base-files kernel-libc-headers"
case "$XBPS_TARGET_MACHINE" in
	*-musl) depends+=" musl-devel musl-legacy-compat";;
	*) depends+=" glibc-devel glibc-locales";;
esac
depends+=" binutils gcc mpfr patch chroot-bash chroot-grep
 coreutils gettext libada-devel gcc-ada chroot-texinfo sed
 chroot-gawk findutils diffutils make gzip file tar chroot-distcc
 chroot-util-linux chroot-git ccache xbps readline xz which"
