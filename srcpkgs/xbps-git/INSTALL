case "${ACTION}" in
post)
	cat <<_EOF
=====================================================================

Don't forget to run 'xbps-pkgdb [-r rootdir] -u' to update the pkg
metadata format to 0.21.

=====================================================================
_EOF
	;;
esac
