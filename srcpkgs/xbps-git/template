# Template file for 'xbps-git'
pkgname=xbps-git
version=20130306
revision=2
build_style=configure
configure_args="--prefix=/ --exec-prefix=/usr --sbindir=/usr/sbin
 --enable-static --enable-debug --enable-tests"
short_desc="The XBPS package system (development branch)"
maintainer="Juan RP <xtraeme@gmail.com>"
homepage="http://code.google.com/p/xbps"
license="Simplified BSD"

nofetch=yes
noextract=yes
provides="xbps-${version}"
replaces="xbps>=0 xbps-devel>=0"
conf_files="/etc/xbps/xbps.conf"
subpackages="libxbps-git libxbps-devel-git xbps-static-git xbps-tests-git"

do_fetch() {
	local url="git://github.com/xtraeme/xbps"
	msg_normal "Fetching source from $url ...\n"
	git clone ${url} ${pkgname}-${version}
}

depends="xbps-triggers libxbps-git-${version}_${revision}"
makedepends="git which pkg-config proplib-devel openssl-devel libfetch-devel
 libarchive-devel>=3.1.2 confuse-devel atf-devel"

if [ -n "$XBPS_CROSS_TRIPLET" ]; then
	makedepends="git which pkg-config"
	crossmakedepends="proplib-devel openssl-devel libfetch-devel
			  libarchive-devel>=3.1.2 confuse-devel atf-devel"
fi
