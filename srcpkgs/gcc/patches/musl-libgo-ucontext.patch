--- libgo/configure
+++ libgo/configure
@@ -13854,6 +13854,11 @@ case "$target" in
 esac
 
 
+case "$target" in
+    *-*-linux-musl*)
+       LIBS="-lucontext $LIBS"
+    ;;
+esac
 
   test -z "$HWCAP_CFLAGS" && HWCAP_CFLAGS=''
 
