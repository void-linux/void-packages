--- lxd/main_forkproxy.go	2018-07-27 03:59:23.000000000 +0300
+++ lxd/main_forkproxy.go	2018-07-30 11:48:45.907000000 +0300
@@ -590,7 +590,7 @@
 			return err
 		}
 
-		errno := C.setgid(C.__gid_t(gid))
+		errno := C.setgid(C.gid_t(gid))
 		if errno < 0 {
 			return fmt.Errorf("setgid: %v", errno)
 		}
@@ -602,7 +602,7 @@
 			return err
 		}
 
-		errno := C.setuid(C.__uid_t(uid))
+		errno := C.setuid(C.uid_t(uid))
 		if errno < 0 {
 			return fmt.Errorf("setuid: %v", errno)
 		}
