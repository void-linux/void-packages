#!/bin/sh
exec chpst -u root kloak 2>&1 1>&1
	echo '#!/bin/sh' > /etc/sv/kloak/log/run
	echo 'exec chpst svlogd -tt /var/log/kloak' >> /etc/sv/kloak/log/run
	chown -R root:root /etc/sv/kloak
	chmod +x /etc/sv/kloak/run
	chmod +x /etc/sv/kloak/log/run
fi
