https://git.savannah.gnu.org/cgit/freedink.git/diff/src/dinkc.c?id=2516bb7c16066d432bf287567f30d533cd067337

--- src/dinkc.c.orig
+++ src/dinkc.c
@@ -749,7 +749,7 @@
 
 void kill_callback(int cb)
 {
-  if (cb >= 0 && cb <= 99)
+  if (cb >= 0 && cb < MAX_CALLBACKS)
     callback[cb].active = /*false*/0;
 }
 
@@ -840,7 +840,7 @@
 	kill_script(k);
     }
   
-  for (k = 1; k <= MAX_CALLBACKS; k++)
+  for (k = 1; k < MAX_CALLBACKS; k++)
     {
       callback[k].active = 0;
     }
