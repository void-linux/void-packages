For some reason this requires swig here but fails to find it,
works fine when removed.
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -1,3 +1,3 @@
 [build-system]
-requires = ["setuptools", "swig"]
+requires = ["setuptools"]
 build-backend = "setuptools.build_meta"
