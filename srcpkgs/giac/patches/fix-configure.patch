--- a/configure.ac
+++ b/configure.ac
@@ -524,8 +524,8 @@
 	      AC_SUBST(FLTK_LIBS)
               cd src && ln -sf ../fltk-1.3.8 && cd ..
            fi
-        fi
             ])
+        fi
 LIBS="$save_LIBS"
 
 AC_CHECK_HEADERS(dfu_lib.h) 
