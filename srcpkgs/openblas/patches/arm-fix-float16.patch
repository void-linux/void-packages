32-bit ARM does not provide a _Float16 type, so fall back to software.

--- a/openblas_config_template.h
+++ b/openblas_config_template.h
@@ -39,7 +39,7 @@
 typedef uint16_t bfloat16;
 #endif
 
-#if defined(__GNUC__) && (__GNUC__ >= 12)
+#if defined(__GNUC__) && (__GNUC__ >= 12) && !(defined(__arm__) && (__ARM_ARCH < 8))
 typedef _Float16 hfloat16;
 #else
 #include <stdint.h>
